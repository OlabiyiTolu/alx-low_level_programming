#include <stdio.h>

/**
 * main - print binary
 * Description: print all binarystarting from zero
 *
 * Return: return 0
 */

int main(void)
{
	int b;

	for (b = 0; b < 10; b++)
	{
		printf("%d", b);
	}
	return (0);
}
